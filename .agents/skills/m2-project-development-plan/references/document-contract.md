# Document Contract — Module Index

This file is a **routing index only** — it carries no schema rules of its
own. Load only the module(s) your task actually needs.

| Module | Load it for |
|---|---|
| [plan-schema.md](plan-schema.md) | Doc purpose, template, output, versioning, and the 12-item section skeleton |
| [plan-sources-normalization.md](plan-sources-normalization.md) | evidence priority and how to write each section well |

Both modules are needed every invocation. `presale-upsell-rules.md` is
separate and situational — read it only when filling/changing the Upsell
section (see `SKILL.md`).
